const express = require("express")

//database validation

const config =  ({
    user: "postgres",
    host: "localhost",
    password: "123456",
    database: "PawPicture"
})

module.exports = config